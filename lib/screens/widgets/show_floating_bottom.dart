import 'package:flutter/material.dart';

class ShowFloatingBottom extends StatelessWidget {
  const ShowFloatingBottom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('data'),
      ],
    );
  }
}
